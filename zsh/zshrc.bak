# Created by newuser for 5.0.5

# Source prezto
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Source oh-my-zsh
if [[ -s "${ZDOTDIR:-$HOME}/.oh-my-zsh/zshrc" ]]; then
  source "${ZDOTDIR:-$HOME}/.oh-my-zsh/zshrc"
fi
